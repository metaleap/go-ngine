/*	Temp file written at runtime for diagnostic purposes.
	The following is a runtime-generated GLSL source string,
	exactly as it was sent to the GL for compilation and linking: */
#version 330 core
#line 1
out vec3 out_Color;
in vec3 var_vec3_TexCube;
uniform samplerCube uni_samplerCube_TexCube;

void fx_TexCube (inout vec3 vCol) {
	vCol = texture(uni_samplerCube_TexCube, var_vec3_TexCube).rgb;
}

void main () {
	vec3 vCol = vec3(0);
	fx_TexCube(vCol);
	out_Color = vCol;
}
